{❘{{{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{d}{2} )}} + {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{f}{2} )}}} )}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}} - {{\cos( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )}} )}} )} + {{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{b - f}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{b + f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}}} )} + {{\cos( \frac{3}{2} )}{\cos( \frac{h}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{b + e}{2} )}} + {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{e}{2} )}}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}}} )}} )}} ){\sin( \frac{g}{2} )}} + {{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}} - {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{b - f}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{b + f}{2} )}}} )}} ){\sin( \frac{g}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}( {{{\cos( \frac{c}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{b + e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{d}{2} )}} + {{\cos( \frac{d}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - f}{2} )}}} )} + {{\sin( \frac{c}{2} )}( {{{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{b}{2} )}( {{{\cos( \frac{d}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{f}{2} )}}} )}} )}} ){\sin( \frac{h}{2} )}} + {{\cos( \frac{g}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}( {{{\cos( \frac{d}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{a}{2} )}} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\sin( \frac{c}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{d}{2} )}} - {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )}} )} - {{\sin( \frac{b}{2} )}( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{c + f}{2} )}}} )}} ){\sin( \frac{h}{2} )}} + {{\sin( \frac{3}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}{\cos( \frac{c + f}{2} )}} + {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\cos( \frac{b + f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{b - f}{2} )}}} )}} ){\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{3}{2} )}( {{{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{b}{2} )}( {{{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}}} )}} )} + {{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}}} ){\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}}}❘}^{2} + {❘{{{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{d}{2} )}} + {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{f}{2} )}}} )}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}} - {{\cos( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )}} )}} )} - {{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{b - f}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{b + f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}}} )} - {{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{b + e}{2} )}} + {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{e}{2} )}}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}}} )}} )}} ){\sin( \frac{g}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{h}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}} - {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{b - f}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{b + f}{2} )}}} )}} ){\sin( \frac{g}{2} )}} - {{\cos( \frac{g}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{c}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{b + e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{d}{2} )}} + {{\cos( \frac{d}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - f}{2} )}}} )} + {{\sin( \frac{c}{2} )}( {{{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{b}{2} )}( {{{\cos( \frac{d}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{f}{2} )}}} )}} )}} ){\sin( \frac{h}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}( {{{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}( {{{\cos( \frac{d}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{a}{2} )}} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\sin( \frac{c}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{d}{2} )}} - {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )}} )} - {{\sin( \frac{b}{2} )}( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{c + f}{2} )}}} )}} ){\sin( \frac{h}{2} )}} + {{\cos( \frac{3}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}{\cos( \frac{c + f}{2} )}} + {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\cos( \frac{b + f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{b - f}{2} )}}} )}} ){\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}} - {{\sin( \frac{3}{2} )}( {{{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{b}{2} )}( {{{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}}} )}} )} + {{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}}} ){\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}}}❘}^{2} + {❘{{{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{c}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{b + e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{d}{2} )}} + {{\cos( \frac{d}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - f}{2} )}}} )} + {{\sin( \frac{c}{2} )}( {{{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{b}{2} )}( {{{\cos( \frac{d}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{f}{2} )}}} )}} )}} )} - {{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}( {{{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}( {{{\cos( \frac{d}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{a}{2} )}} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\sin( \frac{c}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{d}{2} )}} - {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )}} )} - {{\sin( \frac{b}{2} )}( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{c + f}{2} )}}} )}} )} - {{\cos( \frac{3}{2} )}{\cos( \frac{h}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}{\cos( \frac{c + f}{2} )}} + {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\cos( \frac{b + f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{b - f}{2} )}}} )}} ){\sin( \frac{g}{2} )}} + {{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}( {{{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{b}{2} )}( {{{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}}} )}} )} + {{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}}} ){\sin( \frac{g}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{d}{2} )}} + {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{f}{2} )}}} )}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}} - {{\cos( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )}} )}} ){\sin( \frac{h}{2} )}} - {{\cos( \frac{g}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{b - f}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{b + f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}}} ){\sin( \frac{h}{2} )}} - {{\sin( \frac{3}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{b + e}{2} )}} + {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{e}{2} )}}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}}} )}} )}} ){\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{3}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}} - {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{b - f}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{b + f}{2} )}}} )}} ){\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}}}❘}^{2} + {❘{{{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}( {{{\cos( \frac{c}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{b + e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{d}{2} )}} + {{\cos( \frac{d}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - f}{2} )}}} )} + {{\sin( \frac{c}{2} )}( {{{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{b}{2} )}( {{{\cos( \frac{d}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{f}{2} )}}} )}} )}} )} + {{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}( {{{\cos( \frac{d}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{a}{2} )}} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\sin( \frac{c}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{d}{2} )}} - {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )}} )} - {{\sin( \frac{b}{2} )}( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{c + f}{2} )}}} )}} )} + {{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}{\cos( \frac{c + f}{2} )}} + {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\cos( \frac{b + f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{b - f}{2} )}}} )}} ){\sin( \frac{g}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{h}{2} )}( {{{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{b}{2} )}( {{{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}}} )}} )} + {{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}}} ){\sin( \frac{g}{2} )}} - {{\cos( \frac{g}{2} )}{\sin( \frac{3}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{d}{2} )}} + {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{f}{2} )}}} )}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}} - {{\cos( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )}} )}} ){\sin( \frac{h}{2} )}} - {{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{b - f}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{b + f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}}} ){\sin( \frac{h}{2} )}} - {{\cos( \frac{3}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{b + e}{2} )}} + {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{e}{2} )}}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}}} )}} )}} ){\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}} - {{\sin( \frac{3}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}} - {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{b - f}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{b + f}{2} )}}} )}} ){\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}}}❘}^{2} + {❘{{{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{b + e}{2} )}} + {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{e}{2} )}}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}}} )}} )}} )} - {{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}} - {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{b - f}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{b + f}{2} )}}} )}} )} + {{\cos( \frac{3}{2} )}{\cos( \frac{h}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{d}{2} )}} + {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{f}{2} )}}} )}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}} - {{\cos( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )}} )}} ){\sin( \frac{g}{2} )}} - {{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{b - f}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{b + f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}}} ){\sin( \frac{g}{2} )}} - {{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}{\cos( \frac{c + f}{2} )}} + {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\cos( \frac{b + f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{b - f}{2} )}}} )}} ){\sin( \frac{h}{2} )}} + {{\cos( \frac{g}{2} )}{\sin( \frac{3}{2} )}( {{{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{b}{2} )}( {{{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}}} )}} )} + {{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}}} ){\sin( \frac{h}{2} )}} - {{\sin( \frac{3}{2} )}( {{{\cos( \frac{c}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{b + e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{d}{2} )}} + {{\cos( \frac{d}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - f}{2} )}}} )} + {{\sin( \frac{c}{2} )}( {{{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{b}{2} )}( {{{\cos( \frac{d}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{f}{2} )}}} )}} )}} ){\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{3}{2} )}( {{{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}( {{{\cos( \frac{d}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{a}{2} )}} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\sin( \frac{c}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{d}{2} )}} - {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )}} )} - {{\sin( \frac{b}{2} )}( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{c + f}{2} )}}} )}} ){\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}}}❘}^{2} + {❘{{{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{b + e}{2} )}} + {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{e}{2} )}}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}}} )}} )}} )} + {{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}} - {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{b - f}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{b + f}{2} )}}} )}} )} - {{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{d}{2} )}} + {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{f}{2} )}}} )}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}} - {{\cos( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )}} )}} ){\sin( \frac{g}{2} )}} - {{\cos( \frac{3}{2} )}{\cos( \frac{h}{2} )}( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{b - f}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{e}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{b + f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}}} ){\sin( \frac{g}{2} )}} + {{\cos( \frac{g}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}{\cos( \frac{c + f}{2} )}} + {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\cos( \frac{b + f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{b - f}{2} )}}} )}} ){\sin( \frac{h}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}( {{{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{b}{2} )}( {{{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}}} )}} )} + {{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}{\sin( \frac{c + f}{2} )}}} ){\sin( \frac{h}{2} )}} - {{\cos( \frac{3}{2} )}( {{{\cos( \frac{c}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{b + e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{d}{2} )}} + {{\cos( \frac{d}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - f}{2} )}}} )} + {{\sin( \frac{c}{2} )}( {{{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{b}{2} )}( {{{\cos( \frac{d}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}} + {{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{f}{2} )}}} )}} )}} ){\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}} - {{\sin( \frac{3}{2} )}( {{{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}( {{{\cos( \frac{d}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{a}{2} )}} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\sin( \frac{c}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{d}{2} )}} - {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )}} )} - {{\sin( \frac{b}{2} )}( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{e}{2} )}} - {{\cos( \frac{c}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{c + f}{2} )}}} )}} ){\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}}}❘}^{2} + {\frac{1}{128}{❘{{{\sin( \frac{a}{2} )}( {{{\cos( \frac{b}{2} )}( {{- {\cos( {\frac{1}{2}( {c - d - e - f - g - h + 3} )} )}} - {\cos( {\frac{1}{2}( {{- c} + d + e - f - g - h + 3} )} )} + {\cos( {\frac{1}{2}( {c - d - e + f - g - h + 3} )} )} - {\cos( {\frac{1}{2}( {{- c} + d + e + f - g - h + 3} )} )} + {\cos( {\frac{1}{2}( {c + d - e - f + g - h + 3} )} )} + {\cos( {\frac{1}{2}( {{- c} - d + e - f + g - h + 3} )} )} - {\cos( {\frac{1}{2}( {c + d - e + f + g - h + 3} )} )} + {\cos( {\frac{1}{2}( {{- c} - d + e + f + g - h + 3} )} )} - {\cos( {\frac{1}{2}( {c + d - e - f - g + h + 3} )} )} - {\cos( {\frac{1}{2}( {{- c} - d + e - f - g + h + 3} )} )} - {\cos( {\frac{1}{2}( {c + d - e + f - g + h + 3} )} )} + {\cos( {\frac{1}{2}( {{- c} - d + e + f - g + h + 3} )} )} + {\cos( {\frac{1}{2}( {c - d - e - f + g + h + 3} )} )} + {\cos( {\frac{1}{2}( {{- c} + d + e - f + g + h + 3} )} )} + {\cos( {\frac{1}{2}( {c - d - e + f + g + h + 3} )} )} - {\cos( {\frac{1}{2}( {{- c} + d + e + f + g + h + 3} )} )} + {\sin( {\frac{1}{2}( {{- c} - d - e - f - g - h + 3} )} )} + {\sin( {\frac{1}{2}( {c + d + e - f - g - h + 3} )} )} + {\sin( {\frac{1}{2}( {{- c} - d - e + f - g - h + 3} )} )} - {\sin( {\frac{1}{2}( {c + d + e + f - g - h + 3} )} )} - {\sin( {\frac{1}{2}( {{- c} + d - e - f + g - h + 3} )} )} - {\sin( {\frac{1}{2}( {c - d + e - f + g - h + 3} )} )} - {\sin( {\frac{1}{2}( {{- c} + d - e + f + g - h + 3} )} )} + {\sin( {\frac{1}{2}( {c - d + e + f + g - h + 3} )} )} + {\sin( {\frac{1}{2}( {{- c} + d - e - f - g + h + 3} )} )} + {\sin( {\frac{1}{2}( {c - d + e - f - g + h + 3} )} )} - {\sin( {\frac{1}{2}( {{- c} + d - e + f - g + h + 3} )} )} + {\sin( {\frac{1}{2}( {c - d + e + f - g + h + 3} )} )} - {\sin( {\frac{1}{2}( {{- c} - d - e - f + g + h + 3} )} )} - {\sin( {\frac{1}{2}( {c + d + e - f + g + h + 3} )} )} + {\sin( {\frac{1}{2}( {{- c} - d - e + f + g + h + 3} )} )} - {\sin( {\frac{1}{2}( {c + d + e + f + g + h + 3} )} )}} )} + {{\sin( \frac{b}{2} )}( {{\cos( {\frac{1}{2}( {c - d - e - f - g - h + 3} )} )} + {\cos( {\frac{1}{2}( {{- c} + d + e - f - g - h + 3} )} )} - {\cos( {\frac{1}{2}( {c - d - e + f - g - h + 3} )} )} + {\cos( {\frac{1}{2}( {{- c} + d + e + f - g - h + 3} )} )} - {\cos( {\frac{1}{2}( {c + d - e - f + g - h + 3} )} )} - {\cos( {\frac{1}{2}( {{- c} - d + e - f + g - h + 3} )} )} + {\cos( {\frac{1}{2}( {c + d - e + f + g - h + 3} )} )} - {\cos( {\frac{1}{2}( {{- c} - d + e + f + g - h + 3} )} )} + {\cos( {\frac{1}{2}( {c + d - e - f - g + h + 3} )} )} + {\cos( {\frac{1}{2}( {{- c} - d + e - f - g + h + 3} )} )} + {\cos( {\frac{1}{2}( {c + d - e + f - g + h + 3} )} )} - {\cos( {\frac{1}{2}( {{- c} - d + e + f - g + h + 3} )} )} - {\cos( {\frac{1}{2}( {c - d - e - f + g + h + 3} )} )} - {\cos( {\frac{1}{2}( {{- c} + d + e - f + g + h + 3} )} )} - {\cos( {\frac{1}{2}( {c - d - e + f + g + h + 3} )} )} + {\cos( {\frac{1}{2}( {{- c} + d + e + f + g + h + 3} )} )} + {\sin( {\frac{1}{2}( {{- c} - d - e - f - g - h + 3} )} )} + {\sin( {\frac{1}{2}( {c + d + e - f - g - h + 3} )} )} + {\sin( {\frac{1}{2}( {{- c} - d - e + f - g - h + 3} )} )} - {\sin( {\frac{1}{2}( {c + d + e + f - g - h + 3} )} )} - {\sin( {\frac{1}{2}( {{- c} + d - e - f + g - h + 3} )} )} - {\sin( {\frac{1}{2}( {c - d + e - f + g - h + 3} )} )} - {\sin( {\frac{1}{2}( {{- c} + d - e + f + g - h + 3} )} )} + {\sin( {\frac{1}{2}( {c - d + e + f + g - h + 3} )} )} + {\sin( {\frac{1}{2}( {{- c} + d - e - f - g + h + 3} )} )} + {\sin( {\frac{1}{2}( {c - d + e - f - g + h + 3} )} )} - {\sin( {\frac{1}{2}( {{- c} + d - e + f - g + h + 3} )} )} + {\sin( {\frac{1}{2}( {c - d + e + f - g + h + 3} )} )} - {\sin( {\frac{1}{2}( {{- c} - d - e - f + g + h + 3} )} )} - {\sin( {\frac{1}{2}( {c + d + e - f + g + h + 3} )} )} + {\sin( {\frac{1}{2}( {{- c} - d - e + f + g + h + 3} )} )} - {\sin( {\frac{1}{2}( {c + d + e + f + g + h + 3} )} )}} )}} )} + {{\cos( \frac{a}{2} )}( {{\cos( {\frac{1}{2}( {{- b} - c - d - e - f - g - h + 3} )} )} - {\cos( {\frac{1}{2}( {b - c + d + e - f - g - h + 3} )} )} + {\cos( {\frac{1}{2}( {{- b} + c - d - e + f - g - h + 3} )} )} + {\cos( {\frac{1}{2}( {b + c + d + e + f - g - h + 3} )} )} + {\cos( {\frac{1}{2}( {{- b} - c + d - e - f + g - h + 3} )} )} - {\cos( {\frac{1}{2}( {b - c - d + e - f + g - h + 3} )} )} + {\cos( {\frac{1}{2}( {{- b} + c + d - e + f + g - h + 3} )} )} + {\cos( {\frac{1}{2}( {b + c - d + e + f + g - h + 3} )} )} - {\cos( {\frac{1}{2}( {{- b} - c + d - e - f - g + h + 3} )} )} + {\cos( {\frac{1}{2}( {b - c - d + e - f - g + h + 3} )} )} + {\cos( {\frac{1}{2}( {{- b} + c + d - e + f - g + h + 3} )} )} + {\cos( {\frac{1}{2}( {b + c - d + e + f - g + h + 3} )} )} - {\cos( {\frac{1}{2}( {{- b} - c - d - e - f + g + h + 3} )} )} + {\cos( {\frac{1}{2}( {b - c + d + e - f + g + h + 3} )} )} + {\cos( {\frac{1}{2}( {{- b} + c - d - e + f + g + h + 3} )} )} + {\cos( {\frac{1}{2}( {b + c + d + e + f + g + h + 3} )} )} - {\sin( {\frac{1}{2}( {{- b} - c - d - e - f - g - h + 3} )} )} + {\sin( {\frac{1}{2}( {b - c + d + e - f - g - h + 3} )} )} + {\sin( {\frac{1}{2}( {{- b} + c - d - e + f - g - h + 3} )} )} + {\sin( {\frac{1}{2}( {b + c + d + e + f - g - h + 3} )} )} - {\sin( {\frac{1}{2}( {{- b} - c + d - e - f + g - h + 3} )} )} + {\sin( {\frac{1}{2}( {b - c - d + e - f + g - h + 3} )} )} + {\sin( {\frac{1}{2}( {{- b} + c + d - e + f + g - h + 3} )} )} + {\sin( {\frac{1}{2}( {b + c - d + e + f + g - h + 3} )} )} + {\sin( {\frac{1}{2}( {{- b} - c + d - e - f - g + h + 3} )} )} - {\sin( {\frac{1}{2}( {b - c - d + e - f - g + h + 3} )} )} + {\sin( {\frac{1}{2}( {{- b} + c + d - e + f - g + h + 3} )} )} + {\sin( {\frac{1}{2}( {b + c - d + e + f - g + h + 3} )} )} + {\sin( {\frac{1}{2}( {{- b} - c - d - e - f + g + h + 3} )} )} - {\sin( {\frac{1}{2}( {b - c + d + e - f + g + h + 3} )} )} + {\sin( {\frac{1}{2}( {{- b} + c - d - e + f + g + h + 3} )} )} + {\sin( {\frac{1}{2}( {b + c + d + e + f + g + h + 3} )} )}} )}}❘}^{2}}
